<%= form_for(@address) do |f| %>
  <% if @address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@address.errors.count, "error") %> prohibited this address from being saved:</h2>

      <ul>
      <% @address.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label t('name') %><br />
  <%= f.text_field :name %>
  
  <br />
  <%= f.label t('description') %><br />
  <%= f.text_area :description, :size => '38x5' %>
  
  <br />
  <%= f.hidden_field :place_id, {:value => params[:place_id].to_i} %>
  
  <br />
  <%= f.label t('main_venue')+'?' %> <%= f.check_box :main_venue %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
