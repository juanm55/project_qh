algo mas para probar las branches

primero ir al directorio de la app

luego:


git branch nombre_deseado => para crear la branch nombre_deseado


git checkout nombre_deseado => cambiar a la branch nombre_deseado


se hacen las modificaciones

se mira que todo este funcionando


git checkout master => volver a la master


git merge nombre_deseado => juntar nombre_deseado con la master


git add . => poner a git a que vigile todos los archivos de la master


git commit -a -m "despues de juntar nombre_deseado con la master" =>subir la master local (que esta con nombre_deseado) para github


git push => para subirlo a github


para descargar de github.com


git pull git@github.com:juanm55/project_qh.git => git pull... con el identificador de read-write que hay en la pagina

Juambo! test owned!

C YA
